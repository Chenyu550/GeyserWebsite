---
title: Geyser 自托管模组服务器设置
description: 学习如何在你的自托管 Fabric 或 NeoForge 服务器上设置 Geyser。
hide_title: true
---

import { Versions } from '@site/src/components/Versions'

:::caution
Geyser-Fabric 和 Geyser-NeoForge **仅** 能在 <Versions platform="java"/> 服务器上运行。
更多详情请参阅 [此页面](/wiki/geyser/supported-versions#fabric-neoforge-servers)。
:::

:::info
Geyser 仅与 **服务端** 模组兼容。需要在客户端安装的模组将无法工作！
:::

1. 从 [下载页面](/download) 下载模组。
2. 将 Geyser 模组 jar 文件放入 `mods` 文件夹，然后重启服务器。**对于 Fabric，你还需要 [FabricAPI](https://modrinth.com/mod/fabric-api)。**
3. 打开你的 Geyser 配置文件，位置在 `/config/Geyser-Fabric/config.yml`（或 `/config/Geyser-NeoForge/config.yml`），找到以下内容：

    ```yaml
    bedrock:
      # 用于监听连接的 IP 地址。
      # 通常，只有当你想要限制可以连接到服务器的 IP 时，才需要更改此设置。
      address: 0.0.0.0

      # Geyser 用于监听传入的基岩版连接的端口。
      # 由于 Minecraft：基岩版使用 UDP，此端口必须允许 UDP 流量。
      port: 19132

      # 一些托管服务每次启动服务器时都会更改你的 Java 端口，并且要求基岩版使用相同的端口。
      # 此选项使每次启动服务器时，基岩版端口与 Java 端口相同。
      clone-remote-port: false
    ```
    至关重要的部分是端口。这是基岩版玩家用来连接到你的服务器的端口！
    如果你启用了 `clone-remote-port`，端口将被覆盖，并使用 Java 端口。
    由于你是自托管，你可以自由选择端口——默认端口是 19132。
    **重要提示**：其他依赖 UDP 端口的服务/模组，如语音聊天或查询，不能与 Geyser 共享端口。

4. 连接到你的服务器

    **在同一网络内本地连接**
    在与服务器相同的设备上，你可以使用 `localhost` 或 `127.0.0.1` 作为地址进行连接。
    请注意：在同一台 Windows 设备上托管和游玩时，你需要 [修复回环](/wiki/geyser/fixing-unable-to-connect-to-world#using-geyser-on-the-same-computer)。
    同一本地网络中的其他设备可以使用你的本地 IPv4 进行连接——它以 `10.` 或 `192.168.` 开头。

    **从不同网络连接**
    如果你希望来自网络外部的玩家加入，你需要将 Geyser 运行的端口暴露到互联网。
    为此，你有两种选择：

     - 端口转发：在你的路由器/调制解调器以及 Windows/Linux 防火墙中打开 Geyser 端口（例如 19132）的 UDP 协议。
       我们的 [端口转发指南](/wiki/geyser/port-forwarding) 详细解释了如何做到这一点。
       然后，玩家可以使用你的公网 IPv4 + 端口连接到你的服务器。

     - playit.gg：与其开放端口（这可能不是一个选择/如果你不想暴露你的家庭 IP），你可以使用
       playit.gg 服务为你创建一个隧道来路由流量。请参阅我们的 [playit.gg 指南](/wiki/geyser/playit-gg)。
       Ngrok 将无法工作，因为它仅支持 TCP。

5. 通过在控制台中运行 `geyser connectiontest <ip> <port>` 来验证是否可以从其他网络进行连接。

:::info
要允许基岩版玩家无需登录付费的 Java 版帐户即可加入你的服务器，你可以使用 <a href="/wiki/floodgate/setup/">Floodgate</a>。
:::
